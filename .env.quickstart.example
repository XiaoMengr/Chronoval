# ============================================================
# Chronoval 极简 .env（可整体留空，照抄即可一键启动）
# ------------------------------------------------------------
# 用法：cp .env.quickstart.example .env
# 不填任何变量也能正常启动：打开网页后按「首次运行向导」设置管理员密码，
# 会话密钥会自动生成并持久化，无需手动 openssl。
# ------------------------------------------------------------
# 想改站点标题 / 开地图 / 用 S3 / 固定密钥？→ 见「进阶配置」
# (docs/quickstart-deploy.md 或完整版 .env.example)
# ============================================================

# ---- 建议保留（可留空）----
# 固定随机会话密钥（可选；留空会自动生成）。仅多实例/公网加固时需要。
# 生成：openssl rand -base64 32
# NUXT_SESSION_PASSWORD=

# ---- 以下按需解锁（默认已开箱，无需填写）----
# 站点标题
# NUXT_PUBLIC_APP_TITLE=Chronoval
# 首次向导的管理员邮箱/密码预填（留空则在网页里手输）
# CFRAME_ADMIN_EMAIL=
# CFRAME_ADMIN_PASSWORD=
# 地图浏览拍摄位置（免费选 maplibre）
# NUXT_PUBLIC_MAP_PROVIDER=maplibre
# NUXT_PUBLIC_MAP_MAPLIBRE_STYLE=
# NUXT_PUBLIC_MAPBOX_ACCESS_TOKEN=
# 换端口（改了这里的场景无需动，改 docker-compose 的 ports 左侧即可，一般不用）
# NITRO_PORT=3000